/*
export function someMutation (state) {
}
*/

export function amount(a_state, a_value) {
  a_state.amount = a_value
}

export function fields(a_state, a_value) {
  a_state.fields = a_value
}

export function specialCharset(a_state, a_value) {
  a_state.specialCharset = a_value
}

export function settings_amount(a_state, a_value) {
  a_state.settings_amount = a_value
}

export function settings_fields(a_state, a_value) {
  a_state.settings_fields = a_value
}

export function settings_specialCharset(a_state, a_value) {
  a_state.settings_specialCharset = a_value
}
