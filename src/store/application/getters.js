/*
export function someGetter (state) {
}
*/

export function maximized(a_state, a_getters) {
  return a_state.maximized
}

export function config_dice(a_state, a_getters) {
  return a_state.config_dice
}

export function config_key(a_state, a_getters) {
  return a_state.config_key
}

export function config_word(a_state, a_getters) {
  return a_state.config_word

}
export function config_dice_settings(a_state, a_getters) {
  return a_state.config_dice_settings
}

export function config_key_settings(a_state, a_getters) {
  return a_state.config_key_settings
}

export function config_word_settings(a_state, a_getters) {
  return a_state.config_word_settings
}
