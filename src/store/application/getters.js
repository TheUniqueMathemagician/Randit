/*
export function someGetter (state) {
}
*/

export function config(a_state, a_getters) {
    return a_state.config
}

export function isElectron(a_state, a_getters) {
    return a_state.isElectron
}
